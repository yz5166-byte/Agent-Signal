# 项目配置：所有「可能会调的参数」都放这里，代码里不写死。
# 各项的具体内容在对应开发步骤里再填。

# 数据源的开关与参数（分类、关键词、时间窗口、抓取上限）
sources:
  arxiv:
  github:
  news:
  blogs:

# 精选规则：去重、打分、每个板块保留几条
curate:

# 日报：标题、板块顺序、每个板块的展示字段
report:

# 投递：写文件 / 发邮件的开关
delivery:
